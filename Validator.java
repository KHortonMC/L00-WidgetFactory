import java.util.ArrayList;

public class Validator implements Component {
    public Validator() {}

    /**
     * Start with a boolean set to false and set that boolean to the
     * result given from each element in the ArrayList
     * Print the final result
     */
    public void performTask(ArrayList<Widget> widgets) {}    
}
